# Copy values into workspace-specific env files.
# Server: .env.server.local (or export env vars before starting the server)
# Client: client/.env.local

# See .env.server.example and .env.client.example for full options.
